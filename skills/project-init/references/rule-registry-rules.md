# Rule Registry Rules

`docs/ai/rule-registry.md` is the source of persisted durable rules.

AGENTS.md is the operational rendered view.

When user changes a rule:
1. update rule-registry.md
2. update protected-files.md if path protection changed
3. update architecture-notes.md if architecture changed
4. update AGENTS.md generated blocks
