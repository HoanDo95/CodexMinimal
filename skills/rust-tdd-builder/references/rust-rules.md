# Rust Rules

- prefer `Result` for recoverable failures
- keep visibility narrow
- keep binary entrypoints thin
- keep reusable logic in library modules or internal helpers
- do not introduce framework-specific assumptions without repository evidence
