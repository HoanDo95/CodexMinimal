# Stage Rules

Use the smallest unfinished stage.

- If direction is unclear, start at `brainstorm`.
- If direction is approved but implementation shape is not formalized, start at `spec`.
- If spec is approved but implementation is not decomposed, start at `plan`.
- If a plan already exists, hand off to build and stop using this skill.

Do not skip a stage unless its exit criteria are already satisfied.
