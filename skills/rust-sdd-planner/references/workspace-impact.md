# Workspace Impact

When the repository is a Cargo workspace, state:

- which packages or crates change
- which depend on the changed crate
- whether the change affects public exports
- whether integration tests are needed across crate boundaries
